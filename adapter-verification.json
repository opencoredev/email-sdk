{
  "schemaVersion": 1,
  "liveChecks": {
    "resend": {
      "command": "bun run live:resend",
      "secret": "RESEND_API_KEY",
      "probe": "Authenticated GET /domains; never sends email."
    },
    "sequenzy": {
      "command": "bun run live:sequenzy",
      "secret": "SEQUENZY_API_KEY",
      "probe": "Authenticated account lookup; sending is separately opt-in."
    },
    "jetemail": {
      "command": "bun scripts/check-jetemail-account.ts",
      "secret": "JETEMAIL_API_KEY",
      "probe": "Validation-only POST /email with an empty object; validation responses remain inconclusive because the documented contract does not establish authentication. No usable email is submitted. Sending is separately opt-in."
    },
    "primitive": {
      "command": "bun scripts/check-primitive-account.ts",
      "secret": "PRIMITIVE_API_KEY",
      "probe": "Authenticated GET /account in the shared doctor probe; no send or delivery check. Sending is separately opt-in."
    },
    "lettermint": {
      "command": "bun run live:lettermint",
      "secret": "LETTERMINT_API_TOKEN",
      "probe": "Authenticated GET /ping in the shared doctor probe; no send or delivery check. Sending is separately opt-in."
    },
    "lettr": {
      "command": "bun run live:lettr",
      "secret": "LETTR_API_KEY",
      "probe": "Authenticated GET /auth/check in the shared doctor probe; no send or delivery check. Sending is separately opt-in."
    }
  }
}
