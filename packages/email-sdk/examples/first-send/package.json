{
  "name": "email-sdk-first-send",
  "private": true,
  "type": "module",
  "description": "Standalone Email SDK starter: validate a Resend message without sending, then send explicitly.",
  "scripts": {
    "validate": "node first-send.mjs",
    "send": "node first-send.mjs --send",
    "test": "node --test first-send.check.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@opencoredev/email-sdk": "^1.2.0",
    "dotenv": "^17.4.2"
  }
}
